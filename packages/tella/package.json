{
  "name": "tella",
  "version": "0.0.1",
  "main": "src/index.js",
  "types": "dist/index.d.ts",
  "bin": {
    "tella": "bin/dev.mjs"
  },
  "files": [
    "bin",
    "dist"
  ],
  "engines": {
    "node": ">=14.0"
  },
  "scripts": {
    "dev": "vite",
    "build:server": "TELLA_SSR=true vite build",
    "build:client": "TELLA_SSR=false vite build",
    "build:types": "tsc",
    "build": "pnpm build:server && pnpm build:client && pnpm build:types"
  },
  "dependencies": {
    "@vitejs/plugin-react": "^1.3.0",
    "vite": "^2.9.1"
  },
  "devDependencies": {
    "@types/node": "^17.0.23",
    "typescript": "^4.6.3",
    "vitest": "^0.8.2"
  }
}
